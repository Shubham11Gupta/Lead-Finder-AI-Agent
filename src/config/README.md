# Config (Foundation)

This folder stores runtime configuration templates.

## Notes
1. Keep real secrets out of git.
2. Use example files as templates for local setup.
3. Keep `first_touch_only` enabled in v1.
